desc:

simple script that query's bht and finds common synonyms/related words/similar words
you will be prompted to enter two words.

try out some of the combinations listed in test.txt

requires:

simplejson.py: pip install simplejson
requests.py: pip install requests

run:

> python wordlink.py
